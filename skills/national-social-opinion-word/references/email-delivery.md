# 邮件交付至 917935736@qq.com

仅当用户要求发送本任务成果时读取。此处规定写作与交付衔接，具体发送与异常处置使用当前 `$gmail-send-to-917935736` 技能。

## 授权与准备

- “生成并发邮件”“核验后发到邮箱”是当前任务的发送授权；不重复索要同一授权。“完成确认后发送”则先展示最终文件，等用户确认后发送。不要将 QA 自检冒充用户确认。
- 默认收件人 `917935736@qq.com`。用户指定不同地址时遵从用户，使用适用的通用邮件技能，不能悄悄改回默认地址。
- 检查每份最终文件路径、非零字节数、文件名、MIME 类型与内容版本。发送前文件改变则重新核验。多个指定单篇作为独立附件，不擅自打包或额外添加原始材料、QA 图、脚本。
- 读取当前 Gmail 账号，不沿用过去的发件地址或登录状态。发送前展示发件人、收件人、主题、完整正文和所有准确附件文件名。
- 未指定邮件文字时可用：主题“全国政协社情民意信息—〔主题或批次〕”；正文“您好！随信附上〔主题或批次〕的〔数量〕份 Word 文档，请查收。”发送前用实际内容替换方括号字段。

## 发送与核验

调用可用 Gmail 发送能力。多附件逐个独立 MIME 部件，Word 类型为 `application/vnd.openxmlformats-officedocument.wordprocessingml.document`。传递原文件字节，不将长 base64 经模型手工转抄或从截断输出拼接；程序读取与构造负载后直接传递，避免把整段编码输出到对话。

发送后独立检索 `in:sent to:917935736@qq.com`、准确主题、附件及近期过滤条件；匹配真实消息 ID、SENT 标签、收发件人、主题、全部附件文件名/字节数/MIME 类型。缺任何一份附件不能称批量发送完成。

- `SENT_VERIFIED`：发送结果与独立已发送检索匹配，附件全部一致；表示 Gmail 已记录发送，不代表 QQ 已收悉或用户已阅读。
- `DRAFT`：仅草稿存在。
- `BLOCKED`：登录、权限或服务阻止发送，交付本地文件并说明实际阻碍。
- `UNVERIFIED`：可能已发送但证据不足；不自动重发造成重复，先做只读核验，确需重试再由用户明确指示。

最终分别报告文档完成和邮件状态；无须附上邮件正文全文或内部核验资料。
