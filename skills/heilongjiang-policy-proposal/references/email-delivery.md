# 邮件交付

仅当用户明确要求发送本次成果时适用。默认收件人为 `917935736@qq.com`，读取并使用当前可用的 `$gmail-send-to-917935736`。只调用写作技能、生成Word或维护技能不自动发送，不授予永久发送权限。

生成并发送等明确请求授权本次交付，但须遵守所用工具及邮件技能的发送前确认要求。已有精确收件人、最终正文和附件版本批准且无变化时，不额外重复索取同一批准；明确要求确认后发送时必须等待。另一收件人以当次指示为准，不自动替换成默认邮箱。

发送前完成Word及逐页检查，读取当前发件账号，展示发件人、收件人、主题、完整邮件正文及准确附件文件名。核对非零字节数、SHA-256和DOCX MIME；程序从最终文件读取附件字节，不让模型转抄长base64。文件变化后重新核验。

默认主题为黑龙江省政策建议加具体题名或批次，正文简述实际篇数、文种和用途，不宣称作者已审定或已经政协提交。仅附本次最终Word，批量逐篇附加，不擅自附原稿、资料台账、脚本、图片或ZIP。

发送后独立检索Gmail已发送记录，核对消息ID、SENT标签、收发件人、主题及每个附件的文件名、字节数与MIME。`SENT_VERIFIED`仅证明发件系统记录发送，不证明QQ收悉、阅读或正式报送。仅建立草稿为`DRAFT`；登录或权限受阻时保留本地成果并说明阻碍；结果不明为`UNVERIFIED`，继续只读核验，不自动重发。

邮件技能或服务不可用时，不伪造发送成功，不绕过访问限制；先交付本地文件和明确阻碍。
